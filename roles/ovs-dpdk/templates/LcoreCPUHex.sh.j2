#!/bin/bash

function lcorecpuhex {

python "{{ ovs_dpdk_conf_dir }}"/CPUConvertor.py $(numactl -H | grep cpus | cut -d ":" -f 2 | awk -v cpu="{{ (pmd_cpu_per_numa | int) +1 }}" -v '{ printf( "%s,", $cpu )}' | sed 's/.$//')

}

lcorecpuhex