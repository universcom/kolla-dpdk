#!/bin/bash

function cpuisolated {

echo $(numactl -H | grep cpus | cut -d ":" -f 2 | awk -v cpu="{{ pmd_cpu_per_numa }}" ' { for (x=1; x<=cpu; x++) printf( "%s,", $x )}' | sed 's/.$//')

}

cpuisolated